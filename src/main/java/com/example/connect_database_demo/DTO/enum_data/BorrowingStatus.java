package com.example.connect_database_demo.DTO.enum_data;

public enum BorrowingStatus {
    PENDING,
    CANCELED,
    COMPELETED,
    ACCEPTED,
    UNKNOWN
}

// String -> role = "ADMlN"
// String role == "USER" or == "ADMIN" 
